# Mapping_homework
## 一、Bowtie中BWT的提速性质与内存优化策略
### 1.1 BWT核心提速性质
Bowtie核心采用基于**Burrows-Wheeler变换（BWT）**的FM-index索引结构，利用BWT的两个核心性质实现比对速度的量级提升：
1.  **LF映射（Last-to-Front Mapping）可逆回溯特性**
    LF映射是BWT的核心性质：对于BWT矩阵中第i行的字符，其在排序后后缀数组对应前缀行的位置，等于该字符在BWT列第i行之前的出现次数 + 该字符在排序首列的起始偏移量。
    该性质使Bowtie可从reads的3'端到5'端反向比对，每一步比对都能通过LF映射快速缩小候选匹配的后缀区间，无需遍历全参考基因组，将短序列比对的时间复杂度从传统哈希比对的O(N)（N为参考基因组大小）降至O(M)（M为reads长度），大基因组场景下提速效果极显著。
2.  **后缀数组的压缩定位特性**
    BWT将参考基因组的全量后缀数组转换为压缩的BWT字符串，无需存储完整后缀数组即可完成匹配位点的快速定位，避免了全量后缀数组的巨大IO与内存开销，大幅提升了比对的核心运算速度。

### 1.2 内存需求优化策略
Bowtie通过以下核心策略大幅降低内存占用，实现了在普通计算设备上完成大基因组比对：
1.  **采样后缀数组（Sampled SA）压缩**
    Bowtie不存储完整的后缀数组，仅每隔固定间隔（如32/128个后缀）存储一次SA值，其余位置的SA值通过LF映射回溯计算得到，可将后缀数组的内存占用降低1~2个数量级，是最核心的内存优化手段。
2.  **Rank/Select字典压缩结构**
    对BWT字符串采用分块压缩的秩（Rank）查询结构，而非存储原始字符序列。通过块级累计计数+块内压缩位图的设计，在保证LF映射查询速度的同时，大幅降低了BWT字符串与秩表的内存占用。
3.  **索引按需加载与内存映射**
    对正链/反向互补链的双索引采用按需加载机制，比对时仅加载当前所需的索引片段，而非全量加载；同时支持索引的内存映射（mmap），允许多个进程共享同一份索引内存，进一步降低内存峰值。
4.  **比对过程的内存复用与剪枝**
    比对候选区间采用内存池动态复用，避免频繁内存申请释放与内存碎片；对错配、gap的回溯采用深度优先剪枝策略，仅保留当前有效候选区间，减少无效内存占用。
5.  **参考序列按需加载**
    比对核心阶段无需加载完整参考基因组序列，仅在输出比对结果时，按需加载匹配位点附近的参考序列片段，进一步降低核心比对过程的内存需求。

## 二、Bowtie比对与染色体比对reads数统计
### 2.1 环境前提
- 已安装Bowtie1（经典短序列比对工具）
- 预构建的酵母基因组Bowtie索引：`BowtieIndex/YeastGenome`（索引前缀为`YeastGenome`）
- 待比对单端reads文件：`THA2.fa`（FASTA格式）

### 2.2 比对生成SAM文件
执行以下命令完成比对，输出`THA2.sam`文件：
```bash
# Bowtie1比对命令，-S指定输出SAM格式
bowtie -S BowtieIndex/YeastGenome THA2.fa > THA2.sam
```
- 若使用Bowtie2，对应命令为：`bowtie2 -x BowtieIndex/YeastGenome -U THA2.fa -S THA2.sam`

### 2.3 染色体比对reads数统计
#### 方法1：samtools+系统命令（推荐，高效稳定）
前提：已安装samtools，过滤未比对的reads后按染色体分组计数：
```bash
# -F 4 过滤未比对的reads，提取染色体列后排序计数
samtools view -F 4 THA2.sam | cut -f 3 | sort | uniq -c > chromosome_mapping_counts.txt
```
- 输出文件第一列为比对reads数量，第二列为对应染色体名称。

#### 方法2：纯awk命令（无需额外依赖）
```bash
# 筛选比对上的reads，按染色体累计计数，按数量降序输出
awk '$3 != "*" {count[$3]++} END {for (chr in count) print chr, count[chr]}' THA2.sam | sort -k2,2nr > chromosome_mapping_counts.txt
```

## 三、SAM/BAM格式核心概念问答
### 3.1 什么是CIGAR string？包含什么信息？
**CIGAR string（紧凑型间隙比对报告字符串）**是SAM/BAM文件第6列的核心字段，由一系列「数字+操作符」的单元按顺序组成，用于精准描述reads与参考基因组比对的结构细节。

其包含的核心信息：
1.  比对一致性信息：通过`M`（匹配/错配）、`=`（精确匹配）、`X`（错配）操作符，区分reads与参考序列的匹配、错配位点；
2.  插入缺失（InDel）信息：通过`I`（reads相对于参考的插入）、`D`（reads相对于参考的缺失）操作符，精准描述InDel的位置与长度；
3.  剪切信息：通过`S`（软剪切）、`H`（硬剪切）操作符，描述reads两端未参与比对的碱基长度与位置；
4.  剪接跳过信息：通过`N`（参考序列跳过）操作符，描述RNA-seq比对中内含子等跳过的参考区域长度；
5.  碱基坐标对应关系：CIGAR操作顺序与reads碱基顺序完全对应，结合比对起始位点`POS`，可精准推导reads每个碱基对应的参考基因组坐标，是下游变异检测、定量分析的核心依据。

### 3.2 soft clip的含义与CIGAR表示
**Soft clip（软剪切）**是CIGAR中的比对操作，指reads的5'端或3'端的部分碱基未参与与参考基因组的比对，但这些碱基的序列仍完整保留在SAM文件的`SEQ`（第10列，reads序列）字段中。

- 核心应用场景：当reads末端存在低质量碱基、接头序列或无法比对的序列时，软剪切可避免整条reads比对失败，同时保留末端序列供下游分析；
- 与硬剪切（H）的核心区别：软剪切的碱基仍保留在`SEQ`列中，硬剪切的碱基会从`SEQ`列中完全移除，仅在CIGAR中记录剪切长度。

**CIGAR中的表示方式**：软剪切用大写字母`S`作为操作符，前缀数字为被软剪切的碱基数量，仅能出现在CIGAR字符串的开头或结尾。
- 示例1：`5S30M2S`：reads 5'端5个碱基软剪切，中间30个碱基参与比对，3'端2个碱基软剪切，reads总长度37bp，`SEQ`列保留完整37bp序列；
- 示例2：`10S40M`：reads 5'端10个碱基软剪切，剩余40个碱基完成比对；
- 示例3：`45M5S`：reads 3'端5个碱基软剪切，前45个碱基完成比对。

### 3.3 什么是reads的mapping quality？反映什么信息？
**Mapping quality（比对质量，简称MAPQ）**是SAM/BAM文件第5列的整数值，取值范围0~60，是比对软件对该条reads比对到当前报告位置的可信度、唯一性的量化评分，基于Phred质量值体系计算。

核心计算公式：
$$MAPQ = -10 \times log_{10}(P)$$
其中$P$为该条reads比对到当前位置是错误的概率。例如：MAPQ=30对应比对错误概率0.1%，MAPQ=60对应错误概率百万分之一，为最高可信度。

其反映的核心信息：
1.  **比对的唯一性**：MAPQ最核心的作用是区分唯一比对与多映射reads。可比对到参考基因组多个位置的多映射reads，通常被分配MAPQ=0；仅能比对到唯一位置的reads，会得到高MAPQ。
2.  **比对结果的可信度**：MAPQ量化了比对结果的错误概率，MAPQ越高，比对结果越可靠。下游分析（变异检测、定量、peak calling）通常过滤MAPQ<20/30的reads，以降低假阳性。
3.  **比对的质量损耗**：MAPQ会综合考虑比对中的错配、gap数量、reads碱基质量等因素，即使是唯一比对，若存在多个错配或gap，MAPQ也会相应降低，反映比对可靠性的下降。

### 3.4 仅根据SAM/BAM文件的信息，能否推断出read mapping到的区域对应的参考基因组序列？
**结论：常规场景下无法完整推断，仅在满足特定条件时可推断reads比对覆盖区域的参考序列，无法推断全基因组/整条染色体序列**，分情况详细说明：

#### 1. 完全无法推断的常规场景
绝大多数SAM/BAM文件仅存储reads序列、CIGAR、比对坐标等基础信息，**不包含参考基因组的碱基信息**：
- 若SAM/BAM无`MD`标签（SAM规范的可选标签，用于记录错配与缺失的参考碱基），完全无法推断参考序列。仅通过CIGAR的`M`操作符无法区分匹配与错配，也无法得知缺失位点的参考碱基。
- 即使有`MD`标签，也仅能推断reads比对覆盖到的参考序列，无法推断比对区域上下游、软剪切/插入位点对应的参考序列。

#### 2. 可部分推断的限定条件
仅当SAM/BAM文件同时满足以下条件时，可推断**reads比对覆盖区域的参考基因组序列**：
1.  文件包含`MD`标签，完整记录了错配、缺失位点的参考碱基信息；
2.  具备完整的CIGAR string、比对起始位点`POS`、染色体名称`RNAME`与reads序列`SEQ`。

#### 3. 最终总结
- 无`MD`标签的SAM/BAM：完全无法推断比对区域的参考序列；
- 有`MD`标签的SAM/BAM：仅能推断该reads比对覆盖区域的参考序列，无法推断比对区域外的序列；
- 无论何种情况，仅通过SAM/BAM文件无法推断整条染色体或全基因组的参考序列，必须依赖原始参考基因组FASTA文件。

## 四、BWA软件安装与酵母基因组比对流程
！[](https://github.com/Tw1st3n/bioinformatics-class/blob/main/mapping%20homework.png)
